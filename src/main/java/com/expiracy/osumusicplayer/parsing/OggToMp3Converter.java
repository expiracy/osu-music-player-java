package com.expiracy.osumusicplayer.parsing;

import java.io.File;
/*
public class OggToMp3Converter {

    public static void main(String[] args) {
        if (args.length != 2) {
            System.err.println("Usage: OggToMp3Converter <inputFile.ogv> <outputFile.mp3>");
            System.exit(1);
        }

        String inputFilePath = args[0];
        String outputFilePath = args[1];

        try {
            File input = new File(inputFilePath);
            File output = new File(outputFilePath);

            AudioAttributes audio = new AudioAttributes();
            audio.setCodec("libmp3lame");
            audio.setBitRate(128000); // You can adjust the bit rate as needed

            EncodingAttributes attrs = new EncodingAttributes();
            attrs.setFormat("mp3");
            attrs.setAudioAttributes(audio);

            Encoder encoder = new Encoder();
            encoder.encode(input, output, attrs);

            System.out.println("Conversion completed successfully.");
        } catch (Exception e) {
            e.printStackTrace();
            System.err.println("Error: " + e.getMessage());
        }
    }
}

 */
